// js/config.js
// Shared configuration used across all frontend pages

// Base URL of the backend REST API.
// Change this if your backend runs on a different host/port.
const API_BASE_URL = "http://localhost:5000/api";
